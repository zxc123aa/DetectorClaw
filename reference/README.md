# Reference Assets

This directory holds scientific reference material kept in the repository for reproducibility and implementation comparison.

Structure:

- `matlab/`: MATLAB reference scripts and extracted notes
- `shots/`: tracked example shot assets grouped by shot id
- `backgrounds/`: documentation about external background assets referenced by MATLAB or Python configs

These files are reference inputs, not generated run artifacts.
